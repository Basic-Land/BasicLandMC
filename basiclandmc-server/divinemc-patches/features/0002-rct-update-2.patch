From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: dan28000 <84990628+dan28000@users.noreply.github.com>
Date: Mon, 6 Apr 2026 17:45:14 +0200
Subject: [PATCH] rct-update-2


diff --git a/src/main/java/org/bxteam/divinemc/async/rct/RegionizedChunkTicking.java b/src/main/java/org/bxteam/divinemc/async/rct/RegionizedChunkTicking.java
index f1fce59d8e9b26b6dabe10bf23f8506743baf859..db46800e9838624db66a369bfe06ab31f2cc1760 100644
--- a/src/main/java/org/bxteam/divinemc/async/rct/RegionizedChunkTicking.java
+++ b/src/main/java/org/bxteam/divinemc/async/rct/RegionizedChunkTicking.java
@@ -4,26 +4,12 @@ import ca.spottedleaf.moonrise.common.list.IteratorSafeOrderedReferenceSet;
 import ca.spottedleaf.moonrise.common.util.CoordinateUtils;
 import ca.spottedleaf.moonrise.common.util.TickThread;
 import com.mojang.datafixers.DataFixer;
-import com.mojang.logging.LogUtils;
 import io.papermc.paper.entity.activation.ActivationRange;
 import it.unimi.dsi.fastutil.ints.Int2IntOpenHashMap;
 import it.unimi.dsi.fastutil.ints.IntArrayList;
 import it.unimi.dsi.fastutil.longs.Long2IntOpenHashMap;
 import it.unimi.dsi.fastutil.longs.LongOpenHashSet;
 import it.unimi.dsi.fastutil.objects.ObjectArrayList;
-import java.io.IOException;
-import java.util.ArrayList;
-import java.util.Arrays;
-import java.util.Comparator;
-import java.util.List;
-import java.util.Objects;
-import java.util.Set;
-import java.util.concurrent.CompletableFuture;
-import java.util.concurrent.CompletionException;
-import java.util.concurrent.ConcurrentHashMap;
-import java.util.concurrent.Executor;
-import java.util.concurrent.Executors;
-import java.util.function.Supplier;
 import net.minecraft.server.level.ServerChunkCache;
 import net.minecraft.server.level.ServerLevel;
 import net.minecraft.server.level.ServerPlayer;
@@ -39,7 +25,11 @@ import net.minecraft.world.level.storage.LevelStorageSource;
 import org.bxteam.divinemc.config.DivineConfig;
 import org.bxteam.divinemc.util.NamedAgnosticThreadFactory;
 import org.jetbrains.annotations.NotNull;
-import org.slf4j.Logger;
+
+import java.io.IOException;
+import java.util.*;
+import java.util.concurrent.*;
+import java.util.function.Supplier;
 
 public final class RegionizedChunkTicking extends ServerChunkCache {
     public static final Executor REGION_EXECUTOR = Executors.newFixedThreadPool(DivineConfig.AsyncCategory.regionizedChunkTickingExecutorThreadCount,
@@ -103,7 +93,11 @@ public final class RegionizedChunkTicking extends ServerChunkCache {
             }
 
             final long end = System.nanoTime();
-            region.players().forEach(player -> player.avgTickTimeNanos.add(end - start));
+            region.players().forEach(player -> {
+                player.avgTickTimeNanos.add(end - start);
+                player.lastRegionChunkSize = regionChunksIDs.size();
+                player.lastRegionEntityAmount = region.entities().size();
+            });
             return regionChunksIDs;
         }, REGION_EXECUTOR);
     }
@@ -115,7 +109,7 @@ public final class RegionizedChunkTicking extends ServerChunkCache {
             LOGGER.error("Error during region chunk ticking", ex.getCause());
         }
 
-        if (false && i % 100 == 0 && tickPair.regions().length > 0) {
+        if (i % 100 == 0 && tickPair.regions().length > 0) {
             REGION_EXECUTOR.execute(() -> {
                 StringBuilder sb = new StringBuilder();
                 for (RegionData regionData : tickPair.regions()) {
@@ -238,7 +232,7 @@ public final class RegionizedChunkTicking extends ServerChunkCache {
         }
 
         i++;
-        if (false && i % LOG_INTERVAL == 0) {
+        if (i % LOG_INTERVAL == 0) {
             LOGGER.info("Computed {} regions for {} players", regions.size(), players.size());
             LOGGER.info("region sizes for each region: {}", Arrays.toString(regions.stream().mapToInt(r -> r.chunks().size()).toArray()));
         }
diff --git a/src/main/java/org/bxteam/divinemc/async/rct/RollingLongBuffer.java b/src/main/java/org/bxteam/divinemc/async/rct/RollingLongBuffer.java
index d47003554028a344fdeffe4584d78387dd9a8dcf..6ec65a95aead2fc32fb612ead1425ba7b25f0182 100644
--- a/src/main/java/org/bxteam/divinemc/async/rct/RollingLongBuffer.java
+++ b/src/main/java/org/bxteam/divinemc/async/rct/RollingLongBuffer.java
@@ -2,6 +2,7 @@ package org.bxteam.divinemc.async.rct;
 
 import java.util.Arrays;
 import java.util.OptionalDouble;
+import java.util.OptionalLong;
 import java.util.stream.LongStream;
 
 public final class RollingLongBuffer {
@@ -70,6 +71,38 @@ public final class RollingLongBuffer {
         return Arrays.stream(snapshot, 0, snapshot.length);
     }
 
+    /**
+     * Returns the arithmetic average of the last {@code n} entries as OptionalDouble.
+     * If the buffer is empty, returns OptionalDouble.empty().
+     * If n > size(), averages over the available entries.
+     *
+     * Time: O(n) where n is the requested number of entries (bounded by buffer size).
+     *
+     * @throws IllegalArgumentException if n <= 0
+     */
+    public synchronized OptionalDouble averageLast(int n) {
+        if (n <= 0) throw new IllegalArgumentException("n must be > 0");
+        if (count == 0) return OptionalDouble.empty();
+
+        int use = Math.min(n, count);
+        // accumulate in double to reduce risk of overflow for large sums
+        double acc = 0.0;
+        int start = (head + count - use) % capacity;
+        for (int i = 0; i < use; i++) {
+            acc += buffer[(start + i) % capacity];
+        }
+        return OptionalDouble.of(acc / (double) use);
+    }
+
+    /**
+     * Returns the last (newest) entry as OptionalLong, or OptionalLong.empty() if buffer is empty.
+     */
+    public synchronized OptionalLong last() {
+        if (count == 0) return OptionalLong.empty();
+        int idx = (head + count - 1) % capacity;
+        return OptionalLong.of(buffer[idx]);
+    }
+
     /**
      * Returns the arithmetic average as OptionalDouble; O(1).
      */
diff --git a/src/main/java/org/bxteam/divinemc/command/DivineCommand.java b/src/main/java/org/bxteam/divinemc/command/DivineCommand.java
index 86a95b17965ffe5fb0058f2d88091ebae9b35726..d57e59acd4d03bdebd68bdfbb9d6b93651c17037 100644
--- a/src/main/java/org/bxteam/divinemc/command/DivineCommand.java
+++ b/src/main/java/org/bxteam/divinemc/command/DivineCommand.java
@@ -29,12 +29,14 @@ public final class DivineCommand extends Command {
     private static final DivineSubCommand MSPT_SUBCOMMAND = new MSPTCommand();
     private static final DivineSubCommand RELOAD_SUBCOMMAND = new ReloadCommand();
     private static final DivineSubCommand VERSION_SUBCOMMAND = new VersionCommand();
+    private static final DivineSubCommand TICK_TIMES_SUBCOMMAND = new TickTimes();
     private static final Map<String, DivineSubCommand> SUBCOMMANDS = Util.make(() -> {
         final Map<Set<String>, DivineSubCommand> commands = new HashMap<>();
 
         commands.put(Set.of(MSPTCommand.LITERAL_ARGUMENT), MSPT_SUBCOMMAND);
         commands.put(Set.of(ReloadCommand.LITERAL_ARGUMENT), RELOAD_SUBCOMMAND);
         commands.put(Set.of(VersionCommand.LITERAL_ARGUMENT), VERSION_SUBCOMMAND);
+        commands.put(Set.of(TickTimes.LITERAL_ARGUMENT), TICK_TIMES_SUBCOMMAND);
 
         return commands.entrySet().stream()
                 .flatMap(entry -> entry.getKey().stream().map(s -> Map.entry(s, entry.getValue())))
diff --git a/src/main/java/org/bxteam/divinemc/command/subcommands/TickTimes.java b/src/main/java/org/bxteam/divinemc/command/subcommands/TickTimes.java
new file mode 100644
index 0000000000000000000000000000000000000000..a040ee038ed4c9375342af402efc93be48d33508
--- /dev/null
+++ b/src/main/java/org/bxteam/divinemc/command/subcommands/TickTimes.java
@@ -0,0 +1,121 @@
+package org.bxteam.divinemc.command.subcommands;
+
+import net.kyori.adventure.text.Component;
+import net.kyori.adventure.text.event.ClickCallback;
+import net.kyori.adventure.text.event.ClickEvent;
+import net.kyori.adventure.text.format.NamedTextColor;
+import net.minecraft.server.MinecraftServer;
+import net.minecraft.server.level.ServerPlayer;
+import org.bukkit.command.CommandSender;
+import org.bukkit.craftbukkit.entity.CraftPlayer;
+import org.bukkit.permissions.PermissionDefault;
+import org.bxteam.divinemc.command.DivineCommand;
+import org.bxteam.divinemc.command.DivineSubCommandPermission;
+import org.jetbrains.annotations.NotNull;
+
+import java.text.DecimalFormat;
+import java.time.Duration;
+import java.util.List;
+import java.util.Map;
+import java.util.stream.Collectors;
+
+import static net.kyori.adventure.text.Component.empty;
+import static net.kyori.adventure.text.Component.text;
+import static net.kyori.adventure.text.format.NamedTextColor.*;
+
+public final class TickTimes extends DivineSubCommandPermission {
+    public static final String LITERAL_ARGUMENT = "ticktimes";
+    public static final String PERM = DivineCommand.BASE_PERM + "." + LITERAL_ARGUMENT;
+    private static final DecimalFormat DF = new DecimalFormat("########0.0");
+    private static final Component SLASH = text("/");
+    private static final ClickCallback.Options options = ClickCallback.Options.builder().uses(-1).lifetime(Duration.ofMinutes(1)).build();
+
+    public TickTimes() {
+        super(PERM, PermissionDefault.OP);
+    }
+
+    @Override
+    public boolean execute(@NotNull CommandSender sender, @NotNull String subCommand, String @NotNull [] args) {
+        List<ServerPlayer> onlinePlayers = MinecraftServer.getServer().getPlayerList().players;
+
+        if (onlinePlayers.isEmpty()) {
+            sender.sendMessage(text("No players online.", NamedTextColor.GRAY));
+            return true;
+        }
+
+        Map<String, List<ServerPlayer>> playersByWorld = onlinePlayers.stream()
+            .collect(Collectors.groupingBy(p -> p.level().getWorld().getName()));
+
+        List<Map.Entry<String, List<ServerPlayer>>> sortedWorlds = playersByWorld.entrySet().stream()
+            .sorted((e1, e2) -> Integer.compare(e2.getValue().size(), e1.getValue().size()))
+            .toList();
+
+        sender.sendMessage(text("━━━━━━━━━━━━━ ", GOLD)
+            .append(text("Player Tick Times ", YELLOW))
+            .append(text("(last/1s/5s)", NamedTextColor.GRAY))
+            .append(text(" ━━━━━━━━━━━━━", GOLD)));
+
+        for (int i = 0; i < sortedWorlds.size(); i++) {
+            Map.Entry<String, List<ServerPlayer>> entry = sortedWorlds.get(i);
+            String worldName = entry.getKey();
+            List<ServerPlayer> players = entry.getValue();
+
+            players.sort((p1, p2) -> Double.compare(
+                p2.avgTickTimeNanos.average().orElse(0),
+                p1.avgTickTimeNanos.average().orElse(0)
+            ));
+
+            sender.sendMessage(text("➤ ", YELLOW)
+                .append(text(worldName, GOLD))
+                .append(text(" (" + players.size() + " players)", GRAY)));
+
+            for (ServerPlayer player : players) {
+                double last = player.avgTickTimeNanos.last().orElse(0) * 1.0E-6;
+                double avg1s = player.avgTickTimeNanos.averageLast(20).orElse(0.0) * 1.0E-6;
+                double avg5s = player.avgTickTimeNanos.average().orElse(0.0) * 1.0E-6;
+
+                Component stats = text(" [", GRAY)
+                    .append(getColoredValue(last))
+                    .append(SLASH)
+                    .append(getColoredValue(avg1s))
+                    .append(SLASH)
+                    .append(getColoredValue(avg5s))
+                    .append(text("]", GRAY));
+
+                Component hover = text("Last region size: ", GRAY)
+                    .append(text(player.lastRegionChunkSize, GREEN))
+                    .appendNewline()
+                    .append(text("Last region entity amount: ", GRAY))
+                    .append(text(player.lastRegionEntityAmount, GREEN));
+
+                Component playerLine = text("  - ", GRAY)
+                    .append(text(player.getPlainTextName(), AQUA)
+                        .hoverEvent(text("Click to teleport", GREEN))
+                        .clickEvent(ClickEvent.callback(audience -> {
+                            if (audience instanceof CraftPlayer sourcePlayer) {
+                                MinecraftServer.getServer().execute(() -> sourcePlayer.teleport(player.getBukkitEntity().getLocation()));
+                            }
+                        }, options))
+                    )
+                    .append(stats.hoverEvent(hover));
+
+                sender.sendMessage(playerLine);
+            }
+
+            if (i < sortedWorlds.size() - 1) {
+                sender.sendMessage(empty());
+            }
+        }
+
+        return true;
+    }
+
+    private static Component getColoredValue(double value) {
+        NamedTextColor color = value >= 50 ? RED
+            : value >= 40 ? YELLOW
+              : value >= 30 ? GOLD
+                : value >= 20 ? GREEN
+                  : AQUA;
+        return text(DF.format(value) + "ms", color);
+    }
+}
