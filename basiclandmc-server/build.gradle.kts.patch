--- a/divinemc-server/build.gradle.kts
+++ b/divinemc-server/build.gradle.kts
@@ -33,6 +_,7 @@
 
     val divinemc = forks.register("divinemc") {
         forks = purpur
+        rootDirectory = upstreamsDirectory().map { it.dir("divinemc") }
         upstream.patchRepo("paperServer") {
             upstreamRepo = purpur.patchedRepo("paperServer")
             patchesDir = rootDirectory.dir("divinemc-server/paper-patches")
@@ -46,7 +_,27 @@
         }
     }
 
-    activeFork = divinemc
+    val basiclandmc = forks.register("basiclandmc") {
+        forks = divinemc
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = divinemc.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("basiclandmc-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchRepo("purpurServer") {
+            upstreamRepo = divinemc.patchedRepo("purpurServer")
+            patchesDir = rootDirectory.dir("basiclandmc-server/purpur-patches")
+            outputDir = rootDirectory.dir("purpur-server")
+        }
+        upstream.patchDir("divinemcServer") {
+            upstreamPath = "divinemc-server"
+            excludes = setOf("src/minecraft", "paper-patches", "minecraft-patches", "build.gradle.kts", "build.gradle.kts.patch")
+            patchesDir = rootDirectory.dir("basiclandmc-server/divinemc-patches")
+            outputDir = rootDirectory.dir("divinemc-server")
+        }
+    }
+
+    activeFork = basiclandmc
 
     spigot {
         buildDataRef = "3edaf46ec1eed4115ce1b18d2846cded42577e42"
@@ -134,12 +_,16 @@
         resources { srcDir("../paper-server/src/main/resources") }
         java { srcDir("../purpur-server/src/main/java") }
         resources { srcDir("../purpur-server/src/main/resources") }
+        java { srcDir("../divinemc-server/src/main/java") }
+        resources { srcDir("../divinemc-server/src/main/resources") }
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
         resources { srcDir("../paper-server/src/test/resources") }
         java { srcDir("../purpur-server/src/test/java") }
         resources { srcDir("../purpur-server/src/test/resources") }
+        java { srcDir("../divinemc-server/src/test/java") }
+        resources { srcDir("../divinemc-server/src/test/resources") }
     }
 }
 
@@ -163,7 +_,7 @@
 }
 
 dependencies {
-    implementation(project(":divinemc-api")) // DivineMC
+    implementation(project(":basiclandmc-api")) // BasiclandMC
     implementation("ca.spottedleaf:concurrentutil:0.0.3")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -207,6 +_,11 @@
     implementation("org.agrona:agrona:2.0.1")
     // DivineMC end

+    // BL start - reimplement linear format
+    implementation("com.github.luben:zstd-jni:1.5.6-9")
+    implementation("org.lz4:lz4-java:1.8.0")
+    // BL end
+
     runtimeOnly("org.apache.maven:maven-resolver-provider:3.9.6")
     runtimeOnly("org.apache.maven.resolver:maven-resolver-connector-basic:1.9.18")
     runtimeOnly("org.apache.maven.resolver:maven-resolver-transport-http:1.9.18")
@@ -256,14 +_,14 @@
         val experimental = rootProject.providers.gradleProperty("experimental").get()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "DivineMC", // DivineMC
+            "Implementation-Title" to "BasiclandMC", // BasicLandMC
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "DivineMC", // DivineMC
+            "Specification-Title" to "BasiclandMC", // BasiclandMC
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "BX Team", // DivineMC
-            "Brand-Id" to "bxteam:divinemc", // DivineMC
-            "Brand-Name" to "DivineMC", // DivineMC
+            "Specification-Vendor" to "BL Team", // BasiclandMC
+            "Brand-Id" to "basiclandmc:basiclandmc", // BasiclandMC
+            "Brand-Name" to "BasiclandMC", // BasiclandMC
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
